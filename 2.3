Level2(3)
#include <iostream>
using namespace std;

int main() {
    int a, b, c;
    cout << "Uc eded daxil edin: ";
    cin >> a >> b >> c;

    if (a >= b && a >= c) {
        cout << "En boyuk: " << a;
    }
    else if (b >= a && b >= c) {
        cout << "En boyuk: " << b;
    }
    else {
        cout << "En boyuk: " << c;
    }
    return 0;
}
